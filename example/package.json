{
  "name": "pages-plugins-example",
  "private": true,
  "type": "module",
  "scripts": {
    "build:prod": "npx wrangler pages functions build --outdir ./static/_worker.js/",
    "clean": "rm -rf static/_worker.js",
    "start": "npx wrangler pages dev static"
  },
  "dependencies": {
    "graphql": "^16.3.0",
    "react": "^18.2.0"
  }
}
